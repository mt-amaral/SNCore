@namespace Admin.App.Componentes.Hardware


<MudDialog >
    <TitleContent>
        <MudText Typo="Typo.h6">
            <MudIcon Icon="@Icons.Material.Filled.Create" Class="mr-3 mb-n1" />
            New Hardware?
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudTextField Value="@hardware.Id.ToString()" Label="Server ID" ReadOnly="true" />
        <MudTextField Value="@hardware.Model" Label="Server Name" ReadOnly="true" />
        <MudTextField Value="@hardware.Description" Label="Location" ReadOnly="true" />
        <MudTextField Value="@hardware.Ipv4" Label="IP Address" ReadOnly="true" />
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Error" OnClick="Delete">Delete Hardware</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] MudDialogInstance MudDialog { get; set; }
    [Parameter] public HardwareResponse hardware { get; set; } = new HardwareResponse();

    private void Cancel()
    {
        MudDialog.Cancel();
    }

    private void Delete()
    {
        MudDialog.Close(DialogResult.Ok(hardware));
    }
}