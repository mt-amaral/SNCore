@namespace Admin.App.Componentes.Host


<MudDialog MaxWidth="MaxWidth.Medium" FullWidth="true" Style="width: 600px;">
    <TitleContent>
        <MudText Typo="Typo.h6">
            <MudIcon Icon="@Icons.Material.Filled.DeleteForever" Class="mr-3 mb-n1" />
            Delete Host?
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudTextField Value="@host.Id.ToString()" Label="Server ID" ReadOnly="true" DisableUnderLine="true"/>
        <MudTextField Value="@host.Name" Label="Name" ReadOnly="true" AdornmentColor="Color.Transparent" DisableUnderLine="true" />
        <MudTextField Value="@host.Description" Label="Location" ReadOnly="true" AdornmentColor="Color.Transparent" DisableUnderLine="true" />
        <MudTextField Value="@host.Ipv4" Label="IP Address" ReadOnly="true" AdornmentColor="Color.Transparent" DisableUnderLine="true" />
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Error" OnClick="Delete">Delete Host</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] MudDialogInstance MudDialog { get; set; }
    [Parameter] public HostResponse host { get; set; } = new HostResponse();

    private void Cancel()
    {
        MudDialog.Cancel();
    }

    private void Delete()
    {
        MudDialog.Close(DialogResult.Ok(host));
    }
}